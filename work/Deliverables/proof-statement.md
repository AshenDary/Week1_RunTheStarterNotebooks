# What Are You Proving?

I build AI agent systems that actually hold up outside a demo — the kind with real security boundaries, not just a slick chatbot on top of an API call. I want a recruiter screening candidates for an ML/AI internship to read this and book a 15-minute call with me, after seeing that I've already built a multi-tenant RAG platform with row-level data isolation, hashed API keys, and automatic PII scrubbing — the unglamorous stuff most student projects never touch.

## Why This Needs to Exist

My CV can say "RAG" and "multi-tenant architecture," but it can't show that I actually built the security layer around it — that gap is exactly what this proves.
